const User = require("../model/user/user");
const telegramBot = require("node-telegram-bot-api");

const saveInfoUser = async (req, res) => {
  try {
    //* bot
  } catch (err) {
    console.log("user info is cant save to db");
  }
};

module.exports = { saveInfoUser };
